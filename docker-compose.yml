services:
  app:
    build:
      context: ./liquid-glass-clock
      dockerfile: Dockerfile
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    environment:
      - PORT=${PORT:-3000}
    restart: unless-stopped
